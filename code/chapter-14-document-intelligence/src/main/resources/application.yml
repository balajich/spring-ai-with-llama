spring:
  application:
    name: SmartHR Assistant

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 12MB

  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3.2
          num-predict: 800

server:
  port: 8080
